"use strict";

const expect = require('chai').expect;
const fs = require('fs');
const transformer = require('./../lib/randomize');

describe('Will it transform?!', () => {
  it('should read headers', () => {
    
  })
})
